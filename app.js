// Vue 3 Progressive Enhancement for Personal Website
// Disabled - using vanilla JavaScript instead
const hasVue = false;

// Expose flag for legacy script to avoid duplicate rendering
window.__VUE_APP_ACTIVE__ = hasVue;
